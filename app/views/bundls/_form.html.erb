<!--<%= form_with(model: bundl, local: true) do |form| %>
  <% if bundl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bundl.errors.count, "error") %> prohibited this bundl from being saved:</h2>

      <ul>
      <% bundl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>-->

<%= simple_form_for @bundl do |f| %>
  <%= f.label :title %>
  <%= f.input_field :title %>
  <%= f.label :description %>
  <%= f.input_field :description, as: :text %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
